name: Run unit tests when code pushed to repository
on:
  push:
    branches:
      - '*'

permissions:
  id-token: write  # Required for OIDC
  contents: read

jobs:
  tests:
    name: unit-tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          registry-url: 'https://registry.npmjs.org'

      - name: Configure Git
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

      - name: Install dependencies
        run: npm install  # I wish package-lock.json was in git, cos the it would be "npm ci"

      - name: Build package
        run: npm test

